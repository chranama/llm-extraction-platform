base:
  default_model: fake-extract
  defaults:
    backend: fake
    load_mode: lazy
    capabilities:
      generate: true
      extract: true
    readiness_mode: "off"
    deployment_key: observability-proof
    assessment:
      lane: observability-proof
      policy_schema: policy_decision_v2
      require_for_extract: false
      assessed: true
  models: []

profiles:
  observability-proof:
    default_model: fake-extract
    defaults:
      backend: fake
      load_mode: lazy
      capabilities:
        generate: true
        extract: true
      readiness_mode: "off"
      deployment_key: observability-proof
      assessment:
        lane: observability-proof
        policy_schema: policy_decision_v2
        require_for_extract: false
        assessed: true
    models:
      - id: fake-extract
        backend: fake
        load_mode: lazy
        capabilities:
          generate: true
          extract: true
        readiness_mode: "off"
        deployment_key: observability-proof--fake-extract
        remote:
          output_text: '{"company":"ACME","date":"2026-03-25","total":"10.00"}'
        assessment:
          assessed: true
        notes: "Deterministic fake model for observability and kind-based Phase 2 proof."
