base:
  default_model: fake-extract
  defaults:
    backend: fake
    load_mode: lazy
    capabilities:
      generate: true
      extract: true
    readiness_mode: "off"
    deployment_key: aws-dev
    assessment:
      lane: aws-dev
      policy_schema: policy_decision_v2
      require_for_extract: false
      assessed: true
  models: []

profiles:
  aws-proof:
    default_model: fake-extract
    defaults:
      backend: fake
      load_mode: lazy
      capabilities:
        generate: true
        extract: true
      readiness_mode: "off"
      deployment_key: aws-dev
      assessment:
        lane: aws-dev
        policy_schema: policy_decision_v2
        require_for_extract: false
        assessed: true
    models:
      - id: fake-extract
        backend: fake
        load_mode: lazy
        capabilities:
          generate: true
          extract: true
        readiness_mode: "off"
        deployment_key: aws-dev--fake-extract
        remote:
          output_text: '{"company":"ACME","date":"2026-03-25","total":"10.00"}'
        assessment:
          assessed: true
        notes: "Deterministic fake model for the bounded AWS deployment proof."
