"""Persisted runtime signals and query surfaces.

`telemetry/` stores or queries raw execution signals such as traces and
execution logs. It should not package higher-level replay or regression
artifacts; those belong in `observability/`.
"""
