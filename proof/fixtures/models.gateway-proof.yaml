base:
  default_model: fake-sroie
  defaults:
    backend: fake
    load_mode: lazy
    capabilities:
      generate: true
      extract: true
    readiness_mode: "off"
    deployment_key: gateway-proof
    assessment:
      lane: gateway-proof
      policy_schema: policy_decision_v2
      require_for_extract: false
      assessed: true
  models: []

profiles:
  gateway-proof:
    default_model: fake-sroie
    defaults:
      backend: fake
      load_mode: lazy
      capabilities:
        generate: true
        extract: true
      readiness_mode: "off"
      deployment_key: gateway-proof
      assessment:
        lane: gateway-proof
        policy_schema: policy_decision_v2
        require_for_extract: false
        assessed: true
    models:
      - id: fake-sroie
        backend: fake
        load_mode: lazy
        capabilities:
          generate: true
          extract: true
        readiness_mode: "off"
        deployment_key: gateway-proof--fake-sroie
        remote:
          output_text: '{"company":"ACME","address":"123 Main St","date":"2026-03-19","total":"10.00"}'
        assessment:
          assessed: true
        notes: "Deterministic fake model for gateway-backed live extract proof."
