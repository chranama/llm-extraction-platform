base:
  default_model: fake-extract
  defaults:
    backend: fake
    load_mode: lazy
    capabilities:
      generate: true
      extract: true
    readiness_mode: "off"
    deployment_key: async-proof
    assessment:
      lane: async-proof
      policy_schema: policy_decision_v2
      require_for_extract: false
      assessed: true
  models: []

profiles:
  async-proof:
    default_model: fake-extract
    defaults:
      backend: fake
      load_mode: lazy
      capabilities:
        generate: true
        extract: true
      readiness_mode: "off"
      deployment_key: async-proof
      assessment:
        lane: async-proof
        policy_schema: policy_decision_v2
        require_for_extract: false
        assessed: true
    models:
      - id: fake-extract
        backend: fake
        load_mode: lazy
        capabilities:
          generate: true
          extract: true
        readiness_mode: "off"
        deployment_key: async-proof--fake-extract
        remote:
          output_text: '{"id":"1"}'
        assessment:
          assessed: true
        notes: "Deterministic fake model for async extract proof."
